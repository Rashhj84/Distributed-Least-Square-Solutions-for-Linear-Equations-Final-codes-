# WiFi Consensus
Here the same steps from the LAN consensus is to be followed. Only difference is that here for the connection we use internet instead of Ethernet. We then have to setup VPN and get the IP addresses after connecting to VPN. We then follow the same procedures as of the LAN to observe the consensus. The details for the VPN connection is also included in the document.
